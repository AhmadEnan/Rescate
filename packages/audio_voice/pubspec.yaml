name: audio_voice
description: Module 2 - Audio & Voice Interface (Offline VAD, Piper TTS, Multimodal Audio)
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  # Add Piper TTS and VAD dependencies here later

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

resolution: workspace

