name: p2p_mesh
description: Module 4 - P2P Mesh Connectivity (Bluetooth/Wi-Fi Direct, Micro-payloads)
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mesh_network: ^0.1.3
  path: ^1.9.1
  permission_handler: ^12.0.1
  sqflite: ^2.4.2+1
  uuid: ^4.5.3
  security_crypto:
    path: ../security_crypto
  flutter_blue_plus_winrt: ^0.0.20
  # Add Nearby/Multipeer dependencies here later

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

resolution: workspace

