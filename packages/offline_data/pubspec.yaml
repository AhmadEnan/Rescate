name: offline_data
description: Module 3 - Offline-First Data & Search (SQLite FTS5, MBTiles, ObjectBox/WatermelonDB)
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  # Add SQLite, ObjectBox here later

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

resolution: workspace

